# Playwright
node_modules/
playwright-report/
blob-report/
playwright/.cache/
playwright/.auth/

# CI may create a placeholder file so artifact uploads don't fail
playwright-report/artifacts/.placeholder
